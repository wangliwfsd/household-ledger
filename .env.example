POSTGRES_PASSWORD=请设置一个足够长的随机数据库密码
APP_USERNAME=family
# 保留单引号，避免 bcrypt 哈希中的 $ 被 Docker Compose 解释为变量。
APP_PASSWORD_HASH='$2b$12$请替换为实际的bcrypt哈希'
# 至少 32 个随机字符；修改后所有现有登录会话都会失效。
SESSION_SECRET=请替换为至少32个随机字符的会话密钥
# 通过 Cloudflare Tunnel/HTTPS 访问时保持 true；仅纯 HTTP 局域网调试时设 false。
AUTH_COOKIE_SECURE=true
